/**
 * AISettings — settings are now embedded directly inside PromptCard.
 * This file is kept as a no-op to avoid breaking any future imports.
 */
function AISettings() {
  return null
}

export default AISettings
